#include <bits/stdc++.h>
using namespace std;
int main ()
{
    int t;
    cin>>t;
    while(t--)
    {
        string a,b;
        cin>>a>>b;
        string s = a+a;
        long long ans = 0;
        long long P = 131;
        for(int i = 0;i < a.size();i++)
        {
            ans = s
            ans+=(a[i]-'A'+1)*131;
        }
        for(int j)
    }
}